@model BUA_project.Models.Driver

@{
    ViewData["Title"] = "Edit Driver";
}

<h1>Edit Driver</h1>

<form asp-action="Edit" method="post">

    <input type="hidden"
           asp-for="DriverId" />

    <div asp-validation-summary="ModelOnly"
         class="text-danger">
    </div>


    <div class="mb-3">

        <label asp-for="Name"
               class="form-label">
        </label>

        <input asp-for="Name"
               class="form-control" />

        <span asp-validation-for="Name"
              class="text-danger">
        </span>

    </div>


    <div class="mb-3">

        <label asp-for="LicenseNumber"
               class="form-label">
        </label>

        <input asp-for="LicenseNumber"
               class="form-control" />

        <span asp-validation-for="LicenseNumber"
              class="text-danger">
        </span>

    </div>


    <div class="mb-3">

        <label asp-for="QualificationStatus"
               class="form-label">
        </label>

        <select asp-for="QualificationStatus"
                class="form-select">

            <option value="Qualified">
                Qualified
            </option>

            <option value="Expired">
                Expired
            </option>

            <option value="Pending">
                Pending
            </option>

        </select>

        <span asp-validation-for="QualificationStatus"
              class="text-danger">
        </span>

    </div>


    <div class="mb-3">

        <label asp-for="QualificationValidUntil"
               class="form-label">
        </label>

        <input asp-for="QualificationValidUntil"
               type="date"
               class="form-control" />

        <span asp-validation-for="QualificationValidUntil"
              class="text-danger">
        </span>

    </div>


    <div class="mb-3">

        <label asp-for="UserId"
               class="form-label">
            User
        </label>

        <select asp-for="UserId"
                class="form-select">

            @foreach (var user in ViewBag.Users)
            {
                <option value="@user.UserId">
                    @user.Name
                </option>
            }

        </select>

        <span asp-validation-for="UserId"
              class="text-danger">
        </span>

    </div>


    <button type="submit"
            class="btn btn-primary">
        Save Changes
    </button>

    <a asp-action="Index"
           class="btn btn-secondary">
        Back
    </a>

</form>


@section Scripts
{
    @{
        await Html.RenderPartialAsync(
            "_ValidationScriptsPartial");
    }
}